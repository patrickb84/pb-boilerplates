// utils/index

// import _ from 'lodash';
// import { format } from 'date-fns';

import CustomException from './CustomException';

// const assignCollectionId = (collection, index) => {
//   let c = collection.map(item => item.id);
//   console.log(c);

//   let max = _.max(collection.map(item => item.id));

//   if (isNaN(max)) max = 0;
//   return max + 1;
// };

export { CustomException };
